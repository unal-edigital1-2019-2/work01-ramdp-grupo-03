    #include "stdafx.h"
    #include <iostream>
    #include <fstream>
    using namespace std;
    int main()
    {
      int l=0;
      ofstream file;
      file.open("C:/ruta/archivos/archivo.txt");
      for(int l = 0; l<= 76799; l = l + 1)
    {
        file << "0000\n";
    }
      file.close();
      return 0;
    }
